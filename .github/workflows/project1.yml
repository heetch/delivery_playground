name: project1

on:
  workflow_run:
#  push:
#    branches: [ "main" ]
#    paths:
#      - project1/**
#  pull_request:
#    branches: [ "main" ]
#    paths:
#      - project1/**


jobs:
  go_workflow:
    uses: ./.github/workflows/go.yml
    with:
      directory: project1