name: deploy

on:
  workflow_run:
    workflows:
      - project1
    types:
      - completed
    branches: [ "*" ]
  pull_request:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: dummy
        run: echo deploying
