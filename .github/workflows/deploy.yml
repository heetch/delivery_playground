name: deploy

on:
  workflow_run:
    workflows:
      - ./.github/workflows/go.yml
    types:
      - completed
    branches: [ "*" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: dummy
        run: echo deploying
